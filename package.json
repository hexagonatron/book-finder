{
  "name": "book-finder",
  "version": "1.0.0",
  "description": "An app that allows you to search for and save books you like.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "build": "cd client && npm install && npm run build"
  },
  "author": "Ben Fawcett",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^8.2.0",
    "express": "^4.18.2",
    "mongoose": "^5.9.25",
    "node-fetch": "^2.6.1"
  }
}
